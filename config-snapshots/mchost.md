# Слепок конфигурации хостинга Макхост

Снят 31.07.2026. Read-only: не редактировать руками, снимать заново при изменениях.
Источник — `phpinfo()` на сервере, FTP-листинг и панель управления.

## Тариф и доступы

| | |
|---|---|
| Хостинг | Макхост (mchost.ru), шаред |
| Аккаунт | `a250227` |
| Домен | `trexgo.ru`, алиасов нет |
| IP сервера | `178.208.83.20` |
| Панель | https://cp.mchost.ru (вход через mchost.ru/login) |
| Корень сайта | `/home/httpd/vhosts/trexgo.ru/httpdocs` |

Пароли в репозитории не хранятся — репозиторий публичный.
FTP-доступ лежит в GitHub Secrets, полный список — в памяти Claude.

## PHP

Полная конфигурация — в панели, «Управление конфигурацией PHP». Ниже то, что влияет на работу.

| | |
|---|---|
| Версия | 8.0.29, сборка `/opt/alt/php80` (CloudLinux alt-php) |
| Режим | FastCGI |
| memory_limit | 400M |
| max_execution_time | 60 |
| max_input_time | 120 |
| upload_max_filesize | 512M |
| post_max_size | 512M |
| max_file_uploads | 300 |
| max_input_vars | 10000 |
| allow_url_fopen | On |
| allow_url_include | Off |
| disable_functions | пусто |
| Часовой пояс | Europe/Moscow |
| Временная папка | `/home/httpd/vhosts/trexgo.ru/tmp` (upload, сессии, opcache) |

**Расширения:** mysqli, PDO, pdo_mysql, curl, gd, imagick, mbstring, intl, zip,
openssl, json, mysqlnd. Отдельно подключены `opcache` (zend), `pdo`, `mysqlnd`, `imap`.

Закомментированы и **не работают**: `apcu`, `apc`, `sqlite3`. Секции для них в конфиге есть,
но строки `extension=` отключены — если понадобится кэш в памяти или SQLite, просить хостера.

OPcache включён: 128 МБ, до 100 000 файлов. Байткод кэшируется — при заливке PHP-файлов
изменения могут применяться не мгновенно.

FastCGI означает, что настройки PHP через `.htaccess` не меняются —
только панель или `.user.ini`.

### Что придётся поправить, когда появится PHP-код

Сейчас сайт статический, но при добавлении формы заявки или кабинета это выстрелит:

- **`display_errors = Off`, `log_errors = On`** — ошибки не видны в браузере, искать их в
  `logs/error_log`. Это правильная настройка для боевого сервера, менять не нужно.
- **`default_charset = ""`** — пустая. PHP не будет сам ставить `charset=utf-8` в
  `Content-Type`, кириллица поедет. Указывать кодировку явно в коде или заголовках;
  в `.htaccess` уже стоит `AddDefaultCharset UTF-8` для статики.
- **`request_order = "GP"`** — в `$_REQUEST` попадают только GET и POST, куки нет.
- **`session.cookie_httponly`** пустая, **`session.cookie_secure`** не задана — при работе
  с сессиями выставлять флаги явно, иначе куку можно угнать через JS и она уйдёт по HTTP.
- **`expose_php = On`** — сервер сообщает версию PHP в заголовке `X-Powered-By`.
  Наружу сейчас не видно (nginx срезает), но при своей обработке PHP проверить.
- **`log_errors_max_len = 1024`** — длинные ошибки обрезаются в логе.

## База данных

| | |
|---|---|
| Сервер | Percona Server 5.7.22-22 (MySQL-совместимый) |
| Хост | `a250227.mysql.mchost.ru` — резолвится в `127.0.0.1` |
| Сокет | `/home/mysql/mysql.sock` — быстрее, чем через TCP |
| Доступ снаружи | закрыт, порт 3306 недоступен |
| База | `a250227_trexgo`, пустая |
| Права | ALL PRIVILEGES на свою базу, WITH GRANT OPTION |
| Кодировка по умолчанию | **cp1251** — таблицы создавать явно в utf8mb4 |
| max_connections | 4000 |
| max_allowed_packet | 512 МБ |

## Веб-сервер

Перед Apache стоит nginx — наружу отдаётся только `Server: nginx`.
**Версия Apache неизвестна:** хостер скрывает её и в заголовках, и в `phpinfo`,
и в панели. Поэтому все правила в `.htaccess` пишутся в синтаксисе,
совместимом с 2.2 и 2.4 (`RewriteRule ... [F,L]` вместо `Require all denied`).

Модули подтверждены косвенно, по работе: mod_rewrite, mod_deflate, mod_expires.

Панель хостинга — не Plesk, не ISPmanager, не cPanel: собственная разработка Макхоста.
Публичного API у неё нет, всё разовое делается руками через cp.mchost.ru.

## DNS

NS-серверы домена: `ns1..ns4.mchost.ru` — **зона обслуживается хостером**, регистратор
делегировал её сюда. Значит, поддомены заводятся прямо в панели, без похода к регистратору,
и A-запись появляется автоматически.

`trexgo.ru` и `www.trexgo.ru` → `178.208.83.20`. Других записей нет.

## Доступ к файлам

| Протокол | Статус |
|---|---|
| FTP, порт 21 | работает |
| FTPS (AUTH TLS) | **не поддерживается** — сервер отвечает `500 AUTH not understood` |
| FTPS implicit, порт 990 | закрыт |
| **SFTP, порт 22** | **работает**, OpenSSH 8.7, те же логин и пароль |

Отпечаток ключа хоста (RSA), проверять при первом подключении:

```
rtV6fnGKM/gwusoVZa5nKDnNO9f9e5lTqCuoyh6UC/o=
```

Ключ ed25519 сервер тоже отдаёт, но `libssh2` (на нём работает curl) его не поддерживает —
подключаться по RSA. В FileZilla и WinSCP это ограничение не действует.

Через SFTP виден весь путь `/home/httpd/vhosts/trexgo.ru/`, а не только корень сайта:
это тот же аккаунт, просто без chroot. Полноценного shell при этом нет — только файловые операции.

**Выкладка пока идёт по обычному FTP** — `FTP-Deploy-Action` умеет только его, а он даёт
инкрементальную синхронизацию (заливает лишь изменённые файлы). Цена — пароль ходит открытым
текстом. Переход на SFTP — отдельная задача, см. `docs/README.md`.

## SSL

| | |
|---|---|
| Издатель | Let's Encrypt |
| Выдан | 30.07.2026 |
| Действует до | 28.10.2026 |
| Покрывает | `trexgo.ru`, `www.trexgo.ru` |
| Продление | автоматическое, за неделю до истечения |

**Wildcard не выпущен.** Поддомены (`lk.`, `test.`) этим сертификатом не покрыты —
понадобится перевыпуск или отдельный сертификат.

Продление требует, чтобы путь `/.well-known/` оставался открытым.
В `.htaccess` это учтено — правило блокировки точечных файлов его исключает.

## Структура аккаунта

Полный путь: `/home/httpd/vhosts/trexgo.ru/`. По FTP видно то же самое от корня.

```
httpdocs/          корень сайта — сюда льёт GitHub Actions
  preview/         превью веток, заливает отдельный workflow
logs/              access_log, error_log, mail.log, eaccelerator_log
private/           вне веб-доступа, здесь лежит перенесённый phpinfo
subdomains/        пусто
statistics/        отключена в панели
tmp/               сессии, загрузки, кэш
pd/, cgi-bin/
```

**Поддомен** заводится в панели и появляется как
`subdomains/<имя>/httpdocs` — отдельного FTP-логина у него нет, доступ через тот же аккаунт.
Отдельный FTP-доступ Макхост выдаёт на домен целиком, а не на поддомен.

### Что выкладка удаляет, а что нет

Проверено экспериментом 31.07.2026: `FTP-Deploy-Action` ведёт список залитых им файлов в
`httpdocs/.ftp-deploy-sync-state.json` и трогает **только их**. Посторонняя папка, созданная
руками, пережила выкладку без изменений (`Making changes to 0 files/folders`).

Поэтому `httpdocs/preview/` безопасно жить рядом: боевая выкладка её не сносит.
Обратное тоже верно — если что-то залить руками мимо гита, оно останется навсегда
и о нём никто не вспомнит.

Исключение — параметр `dangerous-clean-slate`: он стирает `server-dir` целиком, игнорируя
`exclude`. Не включать.

## Почта

Почтовый сервер включён, `sendmail` работает (`/usr/sbin/sendmail -t -i`),
SMTP на `localhost:25`. Ящики не заведены.

Лог отправки — `logs/mail.log`, включён `mail.add_x_header`: в каждое письмо
подставляется `X-PHP-Originating-Script`, видно, какой скрипт отправил.
Пригодится, когда заработает форма заявки.

## Что отключено или отсутствует

- Статистика посещаемости — выключена в панели
- Почтовые ящики — не созданы
- Cron — не настроен
- Бэкапы — не проверялись
